# Architecture Notes
## rz 11/2025
for responsivenees, we prefetch data
src/contexts/AppDataProvider prefetches data so it can be passed as components



## Run This

To run this application, install its dependencies:

```
npm i
```

With variables set, start your app:

```
npm run start
```

Navigate to http://localhost:3000 in your browser.

### note
This started as okta signin demo code from okta, was expanded to some fully-functional react router endpoints, running on core-tools:3000 for testing